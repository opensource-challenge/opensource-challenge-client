<h1>Der Adventskalender</h1>
<p>Anzahl Beiträge insgesamt: {{challenge.contributions.length}}</p>
<p class="post-contribution-btn content">
  {{#link-to
    'post-contribution'
    (query-params date=(moment-format now 'YYYY-MM-DD'))
    class="btn-primary"
    }}
    <i class="fa fa-plus"></i>
    Jetzt einen neuen
    {{#if media.isMobile}}<br>{{/if}}
    Beitrag erfassen
  {{/link-to}}
</p>
<div class="advent-calendar">
  {{#each-in calendarDays as |date items|}}
  {{~calendar-day date=date items=items~}}
  {{/each-in}}
</div>
