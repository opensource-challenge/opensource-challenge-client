<div class="page page-day">
  <h1 class="content calendar-day-heading">
    {{~calendar-day date=this.model~}}
    {{moment-format this.model "DD.MM.YYYY"}}
    {{#link-to "post-contribution" (query-params date=this.model)}}
      {{fa-icon "plus"}}
    {{/link-to}}
  </h1>

  {{outlet}}
</div>
